<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.angelzheimer2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/angelzheimerround"
        android:label="@string/app_name"
        android:roundIcon="@drawable/angelzheimerround"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".paciente.NotificationActivity"></activity>
        <activity android:name=".paciente.EditNotesActivity" />
        <activity android:name=".paciente.ActivityAddTask" />
        <activity android:name=".paciente.ActivityMemo" />
        <activity android:name=".paciente.SelectedBooks" />
        <activity android:name=".paciente.ReaderActivity" />
        <activity android:name=".paciente.AddNotesActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".cuidador.ActivityMainMenuCuidador" />
        <activity android:name=".cuidador.ActivityFunctionCuidador" />
        <activity android:name=".paciente.ActivityFunction" />
        <activity android:name=".paciente.ActivityMainMenu" />
        <activity android:name=".comum.Activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />


        <receiver android:name=".extras.AlarmeToDo" />
    </application>

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>